Procedure:
    type: object
    properties:
      # 以下工具路径允许为 string 或 null (对应 YAML 中的空值)
      trim_galore: {type: ["string", "null"]}
indir:
    type: string
    default: "output/raw_fastq"
outdir:
    type: string
    default: "output"
